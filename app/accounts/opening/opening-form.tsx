"use client";
import { companyFetch } from "@/app/company-fetch";
import { FormEvent,useState } from "react";
export default function OpeningForm(){const[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");setError("");const f=new FormData(e.currentTarget);const payload=Object.fromEntries(["asOfDate","cash","bank","inventory","receivables","payables","gstPayable"].map(key=>[key,f.get(key)||"0"]));const response=await companyFetch("/api/accounts/opening",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const body=await response.json();setBusy(false);if(!response.ok){setError(body.message||"Could not save.");return}setMessage(body.message)}return <form className="plain-book-form" onSubmit={submit}><div className="form-grid"><label><span>Books start on</span><input name="asOfDate" type="date" required/></label><label><span>Cash in hand ₹</span><input name="cash" inputMode="decimal" defaultValue="0"/></label><label><span>Bank balance ₹</span><input name="bank" inputMode="decimal" defaultValue="0"/></label><label><span>Stock value ₹</span><input name="inventory" inputMode="decimal" defaultValue="0"/></label><label><span>Customers owe you ₹</span><input name="receivables" inputMode="decimal" defaultValue="0"/></label><label><span>You owe suppliers ₹</span><input name="payables" inputMode="decimal" defaultValue="0"/></label><label><span>GST payable ₹</span><input name="gstPayable" inputMode="decimal" defaultValue="0"/></label></div><div className="auto-book-preview"><div><strong>Commons balances the starting entry</strong><span>The difference is placed in Opening balance equity for your accountant to review.</span></div></div>{error&&<p className="form-error">{error}</p>}{message&&<p className="form-success">{message}</p>}<button className="submit-button" disabled={busy}>{busy?"Saving…":"Set opening balances"}</button></form>}
