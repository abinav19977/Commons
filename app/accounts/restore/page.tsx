import {requireChatGPTUser} from "../../company-auth";import RestoreForm from "./restore-form";export const dynamic="force-dynamic";export default async function RestorePage(){await requireChatGPTUser("/accounts/restore");return <main className="form-shell"><section className="customer-form-wrap accounting-surface"><a className="back-link" href="/accounts/controls">← Safety & access</a><div className="surface-heading"><span>Disaster recovery</span><h1>Verify or restore a backup.</h1><p>Commons checks the file fingerprint and company identity before any record is accepted.</p></div><RestoreForm/></section></main>}
