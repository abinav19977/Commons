"use client";
import { companyFetch } from "@/app/company-fetch";
import { FormEvent, useState } from "react";
export default function ChartAccountForm(){const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const response=await companyFetch("/api/accounts/ledger",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({code:f.get("code"),name:f.get("name"),category:f.get("category")})});const body=await response.json();setBusy(false);setMsg(response.ok?"Category added.":body.message);if(response.ok)setTimeout(()=>location.reload(),500)}return <section className="inline-workspace"><h2>Add a money category</h2><form className="plain-book-form compact" onSubmit={submit}><div className="form-grid"><label><span>Four-digit code</span><input name="code" inputMode="numeric" placeholder="6200" pattern="[0-9]{4}" required/></label><label><span>Category name</span><input name="name" placeholder="Delivery expense" required/></label><label><span>Type</span><select name="category"><option value="expense">Expense</option><option value="income">Income</option><option value="asset">What we own</option><option value="liability">What we owe</option><option value="equity">Owner&apos;s money</option></select></label></div>{msg&&<p className={msg.endsWith("added.")?"form-success":"form-error"}>{msg}</p>}<button className="submit-button" disabled={busy}>{busy?"Saving…":"Add category"}</button></form></section>}
