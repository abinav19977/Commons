"use client";
import { useEffect,useId,useState } from "react";import { Search,SlidersHorizontal } from "lucide-react";
type Filter={label:string;value:string};
export default function ListToolbar({filters=[]}:{filters?:Filter[]}){const id=useId(),[query,setQuery]=useState(""),[filter,setFilter]=useState(""),[sort,setSort]=useState("original");useEffect(()=>{const toolbar=document.getElementById(id);const section=toolbar?.closest(".customer-directory");const list=section?.querySelector<HTMLElement>(".customer-list");if(!list)return;const rows=Array.from(list.querySelectorAll<HTMLElement>(":scope > .customer-row"));rows.forEach((row,index)=>{if(!row.dataset.listIndex)row.dataset.listIndex=String(index);const text=(row.textContent||"").toLowerCase();row.hidden=Boolean(query&&!text.includes(query.toLowerCase())||filter&&!text.includes(filter.toLowerCase()))});rows.sort((a,b)=>{if(sort==="az")return(a.textContent||"").localeCompare(b.textContent||"");if(sort==="za")return(b.textContent||"").localeCompare(a.textContent||"");return Number(a.dataset.listIndex)-Number(b.dataset.listIndex)}).forEach(row=>list.appendChild(row))},[id,query,filter,sort]);return <div className="list-toolbar" id={id}><label><Search/><span className="sr-only">Search</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search this list"/></label>{filters.length>0&&<label><SlidersHorizontal/><span className="sr-only">Filter</span><select value={filter} onChange={e=>setFilter(e.target.value)}><option value="">All records</option>{filters.map(item=><option key={item.label} value={item.value}>{item.label}</option>)}</select></label>}<label><span className="sr-only">Sort</span><select value={sort} onChange={e=>setSort(e.target.value)}><option value="original">Newest first</option><option value="az">A → Z</option><option value="za">Z → A</option></select></label></div>}
